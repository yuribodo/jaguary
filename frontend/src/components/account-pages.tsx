"use client";

import Link from "next/link";
import Cards from "react-19-credit-card";
import { useMemo, useState } from "react";
import { CheckIcon, ExternalLinkIcon, LockKeyholeIcon, PlaneIcon, ReceiptTextIcon, ShieldCheckIcon } from "lucide-react";
import { AccountPageShell } from "@/components/account-page-shell";

type WalletCard = { id: string; brand: "mastercard" | "visa"; lastFour: string; expiry: string; reference: string; limit: number; used: number; usage: string };
const walletCards: WalletCard[] = [
  { id: "mastercard-0446", brand: "mastercard", lastFour: "0446", expiry: "01/31", reference: "cred_demo_marta_mastercard", limit: 8000, used: 1860, usage: "Purchases authorized by mandate" },
  { id: "mastercard-5364", brand: "mastercard", lastFour: "5364", expiry: "05/29", reference: "cred_demo_marta_mastercard_2", limit: 5000, used: 920, usage: "Purchases authorized by mandate" },
  { id: "visa-1855", brand: "visa", lastFour: "1855", expiry: "12/30", reference: "cred_demo_marta_visa", limit: 6500, used: 0, usage: "Purchases authorized by mandate" },
];
const brl = new Intl.NumberFormat("en-US", { style: "currency", currency: "BRL" });

function PageIntro({ eyebrow, title, description }: { eyebrow: string; title: string; description: string }) {
  return <div className="max-w-2xl border-b pb-8"><p className="font-mono text-[10px] tracking-[0.14em] text-muted-foreground uppercase">{eyebrow}</p><h1 className="mt-3 text-4xl leading-none [font-family:var(--font-serif)] md:text-5xl">{title}</h1><p className="mt-5 text-base leading-7 text-muted-foreground">{description}</p></div>;
}

function CardPreview({ card, selected, onSelect }: { card: WalletCard; selected: boolean; onSelect: () => void }) {
  return <button aria-pressed={selected} className="group min-w-[17.75rem] snap-center text-left outline-none focus-visible:ring-4 focus-visible:ring-ring/35" onClick={onSelect} type="button"><div className={selected ? "rounded-xl ring-2 ring-[#334de8] ring-offset-4 ring-offset-background" : "rounded-xl transition-transform duration-200 group-hover:-translate-y-1"}><Cards acceptedCards={[card.brand]} cvc="" expiry={card.expiry} focused="number" issuer={card.brand} name="MARTA ALMEIDA" number={`••••••••••••${card.lastFour}`} preview /></div><div className="mt-4 flex items-center justify-between px-1"><span className="font-mono text-xs tracking-[0.12em]">•••• {card.lastFour}</span><span className="text-xs text-muted-foreground">{selected ? "Selected" : "View details"}</span></div></button>;
}

export function PaymentMethodsPage() {
  const [selectedId, setSelectedId] = useState(walletCards[0].id);
  const selected = walletCards.find((card) => card.id === selectedId) ?? walletCards[0];
  const { total, used } = useMemo(() => walletCards.reduce((acc, card) => ({ total: acc.total + card.limit, used: acc.used + card.used }), { total: 0, used: 0 }), []);
  const selectedUsed = Math.round((selected.used / selected.limit) * 100);
  const totalUsed = Math.round((used / total) * 100);
  return <AccountPageShell activePage="payment-methods"><PageIntro eyebrow="Account / payments" title="Payment methods" description="Select a card to view its limit and how it can be used. Credentials are never exposed." /><section className="mt-8"><div className="mb-4 flex items-baseline justify-between"><h2 className="text-2xl [font-family:var(--font-serif)]">Your wallet</h2><span className="font-mono text-[10px] tracking-[0.12em] text-muted-foreground">{walletCards.length} CARDS</span></div><div className="-mx-4 flex snap-x gap-7 overflow-x-auto px-4 pb-7 md:mx-0 md:px-0"><style jsx global>{`.rccs { margin: 0; width: 100%; } .rccs__card { width: 100%; max-width: 350px; }`}</style>{walletCards.map((card) => <CardPreview card={card} key={card.id} onSelect={() => setSelectedId(card.id)} selected={card.id === selected.id} />)}</div></section><section className="grid gap-6 border-t pt-8 lg:grid-cols-[minmax(0,.9fr)_minmax(18rem,1.1fr)]"><article className="rounded-xl border bg-card p-6 shadow-xs"><p className="font-mono text-[10px] tracking-[0.12em] text-muted-foreground uppercase">Total wallet limit</p><h2 className="mt-2 text-4xl [font-family:var(--font-serif)]">{brl.format(total)}</h2><div className="mt-8"><div className="mb-2 flex justify-between text-sm"><span className="text-muted-foreground">Limit used</span><strong>{brl.format(used)}</strong></div><div aria-label={`${totalUsed}% of the total limit used`} className="h-2 overflow-hidden rounded-full bg-secondary"><div className="h-full rounded-full bg-[#334de8] transition-[width] duration-300" style={{ width: `${totalUsed}%` }} /></div><div className="mt-3 flex justify-between text-xs text-muted-foreground"><span>{totalUsed}% used</span><span>{brl.format(total - used)} available</span></div></div></article><article aria-live="polite" className="rounded-xl border bg-card p-6 shadow-xs"><div className="flex items-start justify-between gap-4"><div><p className="font-mono text-[10px] tracking-[0.12em] text-muted-foreground uppercase">Selected card</p><h2 className="mt-2 text-2xl [font-family:var(--font-serif)]">•••• {selected.lastFour}</h2><p className="mt-1 text-sm text-muted-foreground">{selected.brand === "visa" ? "Visa" : "Mastercard"} · expires {selected.expiry}</p></div><span className="inline-flex items-center gap-1 rounded-full bg-emerald-950/8 px-2 py-1 font-mono text-[10px] text-emerald-900"><CheckIcon className="size-3" /> ACTIVE</span></div><div className="mt-6"><div className="mb-2 flex justify-between text-sm"><span className="text-muted-foreground">Limit used</span><strong>{brl.format(selected.used)}</strong></div><div aria-label={`${selectedUsed}% of the card limit used`} className="h-2 overflow-hidden rounded-full bg-secondary"><div className="h-full rounded-full bg-[#334de8] transition-[width] duration-300" style={{ width: `${selectedUsed}%` }} /></div><p className="mt-3 text-xs text-muted-foreground">{brl.format(selected.limit - selected.used)} available out of {brl.format(selected.limit)}</p></div><dl className="mt-6 grid gap-3 border-t pt-4 text-sm"><div className="flex justify-between gap-4"><dt className="text-muted-foreground">Reference</dt><dd className="font-mono text-xs">{selected.reference}</dd></div><div className="flex justify-between gap-4"><dt className="text-muted-foreground">Use</dt><dd className="max-w-[15rem] text-right">{selected.usage}</dd></div></dl></article></section><p className="mt-5 flex items-start gap-2 text-xs leading-5 text-muted-foreground"><LockKeyholeIcon className="mt-0.5 size-3.5 shrink-0" />The card is a secure preview: no full number, CVV, or payment token is exposed.</p></AccountPageShell>;
}

export function PurchasesPage() {
  const purchases = [
    { route: "São Paulo → Rio", amount: 600, date: "Aug 18, 2026", card: "•••• 0446", receipt: "vy_7B91K" },
    { route: "Mexico City → Bogotá", amount: 2300, date: "Aug 12, 2026", card: "•••• 5364", receipt: "vy_42MQA" },
    { route: "São Paulo → Buenos Aires", amount: 1600, date: "Aug 3, 2026", card: "•••• 1855", receipt: "vy_9P3LD" },
    { route: "Buenos Aires → Mexico City", amount: 3700, date: "Jul 27, 2026", card: "•••• 0446", receipt: "vy_1H8RX" },
  ];
  const total = purchases.reduce((sum, purchase) => sum + purchase.amount, 0);
  return <AccountPageShell activePage="purchases"><PageIntro eyebrow="Account / history" title="Purchases" description="Completed VuelaYa trips. Each receipt remains separate from the mandates and checkouts that preceded it." /><section className="mt-8"><div className="mb-5 flex items-baseline justify-between"><div><p className="font-mono text-[10px] tracking-[0.12em] text-muted-foreground uppercase">Selected period</p><h2 className="mt-1 text-2xl [font-family:var(--font-serif)]">August 2026</h2></div><p className="text-sm text-muted-foreground"><strong className="text-foreground">{brl.format(total)}</strong> across 4 purchases</p></div><div className="grid gap-4">{purchases.map((purchase) => <article className="grid gap-4 rounded-xl border bg-card p-5 shadow-xs sm:grid-cols-[auto_minmax(0,1fr)_auto] sm:items-center" key={purchase.receipt}><span className="grid size-11 place-items-center rounded-full border bg-background text-[#334de8]"><PlaneIcon className="size-5" /></span><div className="min-w-0"><div className="flex flex-wrap items-center gap-x-3 gap-y-1"><h3 className="text-lg [font-family:var(--font-serif)]">{purchase.route}</h3><span className="inline-flex items-center gap-1 rounded-full bg-emerald-950/8 px-2 py-0.5 font-mono text-[10px] text-emerald-900"><CheckIcon className="size-3" /> COMPLETED</span></div><p className="mt-1 text-sm text-muted-foreground">VuelaYa · {purchase.date} · {purchase.card}</p><code className="mt-2 block text-[11px] text-muted-foreground">Receipt {purchase.receipt}</code></div><strong className="text-lg sm:text-right">{brl.format(purchase.amount)}</strong></article>)}</div></section><p className="mt-5 flex items-start gap-2 text-xs leading-5 text-muted-foreground"><ReceiptTextIcon className="mt-0.5 size-3.5 shrink-0" />Fictional history for visual demonstration. It does not represent real transactions.</p></AccountPageShell>;
}

export function ConnectedMerchantsPage() {
  return <AccountPageShell activePage="merchants"><PageIntro eyebrow="Account / trust" title="Connected merchants" description="Merchants can only receive authority when they are identified and within the scope defined by the mandate." /><section className="mt-8 rounded-xl border bg-card p-5 shadow-xs"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><h2 className="font-medium">VuelaYa</h2><ShieldCheckIcon className="size-4 text-emerald-800" /></div><p className="mt-1 text-sm text-muted-foreground">Airline · merchant_vuelaya</p></div><span className="rounded-full bg-secondary px-2 py-1 font-mono text-[10px]">CONNECTED</span></div><dl className="mt-5 grid gap-3 border-t pt-4 text-sm sm:grid-cols-2"><div><dt className="text-muted-foreground">Capability</dt><dd className="mt-1">AP2 mandate</dd></div><div><dt className="text-muted-foreground">Protocol</dt><dd className="mt-1">UCP 2026-08-25</dd></div></dl><Link className="mt-5 inline-flex items-center gap-1 text-sm text-blue-800 hover:underline" href="/connected-merchants/vuelaya">View merchant identity <ExternalLinkIcon className="size-3" /></Link></section></AccountPageShell>;
}
